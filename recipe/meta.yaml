{% set name = "tig" %}
{% set version = "2.6.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/jonas/{{ name }}/archive/refs/tags/{{ name }}-{{ version }}.tar.gz
  sha256: 686967750b9b6331b9f8dedff73ca2e3a5eba3545f77a5af24e42ce6f0527a4c

build:
  number: 0
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - autoconf
    - automake
    - pkg-config
    - make
  host:
    - pcre
    - ncurses

test:
  commands:
    - tig --version

about:
  home: https://jonas.github.io/tig/
  dev_url: https://github.com/jonas/tig
  summary: Text-mode interface for git
  license: GPL-2.0-or-later
  license_file: COPYING

extra:
  recipe-maintainers:
    - wolfv
    - tdejager
    - dbast
